#Author: Everton da Silva
#Date: 2021, July

from sklearn import preprocessing
import pandas as pd


def nomalizador_minMaxScaler(dataframe, columns):
    print("nomalizador_minMaxScaler(dataframe, columns)")

def classifier_sklearn(dataframe, column_name):
    print("classifier_sklearn(dataframe, column_name)")
    new_dataframe = pd.DataFrame()
    #dados_cat_normalizados = pd.get_dummies(dados_cat, prefix="sexo")
    #print(dados_cat_normalizados)
    return new_dataframe